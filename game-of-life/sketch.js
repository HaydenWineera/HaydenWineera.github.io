// game of life
let gridSize = 10;
let grid;
function setup() {
  createCanvas(windowWidth, windowHeight);
  createEmpty2DArray(gridSize, gridSize);
}

function draw() {
  background(220);


}

function createEmpty2DArray(rows, cols) {
  
}